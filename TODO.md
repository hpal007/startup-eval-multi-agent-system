commands 
- `uvicorn main:app --reload`
- cd ui and run `npm run dev`

-- ruff
`v sync --extra dev`
`uv run ruff check`
`v run ruff check --fix`

backend 
- issues in founder competitor profile and bussiness kpis(some part works) - files are not geting downloaded. (need to do one by one testing)

ui
- changes needed